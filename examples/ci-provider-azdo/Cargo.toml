[package]
name = "atm-ci-provider-azdo"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[lib]
crate-type = ["cdylib"]

# Exclude this example from the parent workspace
[workspace]

[dependencies]
agent-team-mail-ci-monitor = { path = "../../crates/atm-ci-monitor" }
