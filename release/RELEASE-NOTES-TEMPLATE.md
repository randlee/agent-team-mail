# Release Notes Template

Fill this out and provide to `publisher` before release merges to `main`.
Publisher will set this as the GitHub Release body after the workflow completes.

---

## v{VERSION}

{ONE_SENTENCE_SUMMARY — e.g. "This release covers Phases XX through YY, delivering ..."}

### What's New

- **{Feature name}** — {One sentence description} (Phase {XX})
- **{Feature name}** — {One sentence description} (Phase {XX})
- **{Feature name}** — {One sentence description} (Phase {XX})

### New Crates
<!-- Remove section if none -->
- `{crate-name}` — {one line description}

### Fixes
<!-- Remove section if none -->
- {Fix description} (#{issue})

### Breaking Changes
<!-- Remove section if none -->
- {Description}

### Published Crates (all {VERSION})
<!-- List in publish order, one per line -->
- `{crate-name}`
- `{crate-name}`

---
<!-- Do not edit below this line — publisher fills in checksums from workflow artifacts -->
